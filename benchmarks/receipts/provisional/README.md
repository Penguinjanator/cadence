These completed measurements precede the frozen-archive replay check. Their numerical
controls and arithmetic verification passed, but the initial source packager omitted
the three `src/cadence/circuits/*.py` modules, preventing standalone replay. Their archive
and measurements are retained here; the final receipts one directory above rerun the
same declared protocol with a complete recursive source archive. Do not cite these as
standalone replayable bundles. The numerical kernels did not change in this correction.
